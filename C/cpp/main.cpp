#include <bits/stdc++.h>

using namespace std;

ifstream f("in.txt");
ofstream g("out.txt");

int main() {
  int n, m{};
  vector<int> nums;

  cin >> n >> m;
  for (int i = 0; i < n; i++) {
    int temp;
    cin >> temp;
    nums.push_back(temp);
  }
}
